node_modules
dist
release
.env
.env.local
.DS_Store
*.log

# Local Netlify folder
.netlify
.vercel
.env*.local

# One-off maintenance scripts that hardcode a service_role key (old iqvbv project).
# Ignored so they can never be accidentally committed/published.
scripts/*.cjs
